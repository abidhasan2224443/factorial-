/* 
#include <stdio.h>
int sum (int n){

    int f=0;
    int i;
    for ( i=1; i<=n; i++) {

   f=f+i; }

    return f;
}
int main (){

    int n;

    printf("koto porjonto jog korte cao ? \n=  ");

    scanf("%d", &n);

    printf("sum of %d number is ", n);

    printf(" %d", sum(n));

    return 0;
}
    */